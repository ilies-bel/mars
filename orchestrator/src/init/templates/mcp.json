{
  "_comment": "INTERACTIVE-SESSION config only. Mars WORKERS no longer receive the codegraph MCP server at all — they invoke the codegraph CLI directly via guidance in CODEGRAPH_CLI_SYSTEM_PROMPT (src/core/lib/git/claude.ts), which eliminates the fixed token tax of injecting MCP schemas + instructions on every task. Do not add this MCP config to the worker dispatch path.",
  "mcpServers": {
    "codegraph": {
      "type": "stdio",
      "command": "codegraph",
      "args": ["serve", "--mcp"]
    }
  }
}
