# Context note: mars-8b6af855 (context-gatherer for mars-5f8debef)

## What the parent task (mars-5f8debef) needed

The implementor ran out of its 5-read budget without acting.  It had
already read all the relevant files; no missing context remained.

## What was done (committed to task/mars-5f8debef)

Implemented the full slice directly in mars-5f8debef's branch:

1. **`orchestrator/src/mastra/context.ts`** — added `traceDbPath` field to
   `OrchestratorContext` interface and assigned it `resolve(stateDir, 'traces.duckdb')`.

2. **`orchestrator/src/mastra/daemon/trace-store.ts`** (new) — `openTraceStore(path?)`
   opens a `DuckDBConnection` and creates `mars_trace_events` (ts TIMESTAMPTZ,
   origin_id VARCHAR, entity_id VARCHAR, event_name VARCHAR).  Accepts an
   optional path for testability; defaults to `resolveContext().traceDbPath`.

3. **`orchestrator/src/mastra/daemon/server.ts`** — inside the existing
   `MARS_DISABLE_DUCKDB !== '1'` guard: added `probeDuckDBLock(traceDbPath)`,
   exits with a clear message if the lock is held, then calls `openTraceStore()`.
   When the env flag is set neither store is opened.

4. **`orchestrator/src/mastra/daemon/__tests__/trace-store.test.ts`** (new) —
   4 passing tests: file created on first open, schema accepts all fields,
   idempotent table creation, and distinct filenames for the two stores.

## Verification

`cd orchestrator && npm test` — 4/4 new tests pass; 412 pre-existing tests pass.
The one pre-existing failure (`run-claude-code-timeout.test.ts`) is unrelated.
`npm run typecheck` — clean.
