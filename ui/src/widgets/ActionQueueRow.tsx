/**
 * ActionQueueRow widget — re-exported from its page module so callers can
 * import from either the canonical widgets path or the page directly.
 *
 * The component is authored in ActionQueuePage to co-locate it with the
 * page-level handler dependencies (handleInlineAction, handleRestart, etc.).
 */
export { ActionQueueRow } from '@/pages/ActionQueuePage'
