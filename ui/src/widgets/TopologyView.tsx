/**
 * Topology view — renders a DAG of blocker edges between queued/running/done
 * tasks. This is the shell introduced in slice 2; subsequent slices add the
 * actual graph rendering.
 */
export const TopologyView = () => (
  <main className="flex min-h-0 flex-1 items-center justify-center overflow-hidden bg-bg">
    <p className="font-mono text-[13px] text-iron">
      Topology — coming soon
    </p>
  </main>
)
