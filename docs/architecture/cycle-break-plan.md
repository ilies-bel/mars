# Cycle-Break Plan

**Status:** DRAFT — planning only. No cycles are broken by this document.
**Base commit:** `69d4b45d`
**Scope:** the concrete, ordered sequence of commits that dismantles the
import cycles measured by dependency-cruiser 18.1.0 on this tree.

> SKELETON COMMITTED FIRST. Sections are filled in progressively; any
> section still reading "NOT VERIFIED — needs a pass" was not read at
> source level and must not be trusted.

## 0. Baseline facts

## 1. The 34-file SCC (core/arc.ts, core/queue.ts, core/lib/action-queue.ts, ...)

## 2. The 5-file RPC cycle

## 3. The 4-file provider cycle

## 4. The two 3-file cycles

## 5. The 6-folder UI SCC

## 6. The sequence

## 7. Reality check
